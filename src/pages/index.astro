---
import { loadCapturedPages } from "../lib/captured-pages";

const homePage = loadCapturedPages().find((page) => page.pathname === "/");

if (!homePage) {
  throw new Error("Missing captured homepage entry for '/'.");
}
---

<Fragment set:html={homePage.rawHtml} />
