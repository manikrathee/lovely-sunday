---
import AnnouncementBar from "./AnnouncementBar.astro";

const utilities = [
  { icon: "◉", label: "Browse our social feed" },
  { icon: "⌕", label: "Search LovelySunday archives" },
  { icon: "👜", label: "View cart" },
];
---

<header class="site-head">
    <AnnouncementBar />

    <div class="site-head-container">
        <button class="icon-button" aria-label="Open menu">☰</button>

        <a class="site-head-logo" href="/" aria-label="Lovely Sunday home">
            <img src="/img/lovely-sunday-logo.svg" alt="Lovely Sunday" />
        </a>

        <div class="site-head-right" aria-label="Utilities">
            {utilities.map((utility) => (
                <button
                    type="button"
                    class="icon-button"
                    aria-label={utility.label}
                >
                    {utility.icon}
                </button>
            ))}
        </div>
    </div>
</header>
